import React from 'react';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { Badge, Card } from '../components/ui';
import { colors, spacing, typography } from '../theme';
export const typeLabel = { contrato: 'Contrato', exame: 'Exame', boleto: 'Boleto', termo_de_uso: 'Termo de uso', outro: 'Outro' };
export const date = value => value ? new Intl.DateTimeFormat('pt-BR', { day: '2-digit', month: 'short' }).format(new Date(value)) : '—';
export function DocumentCard({ document, onPress, onDelete }) { return <Pressable onPress={onPress} accessibilityRole="button" accessibilityLabel={`Abrir ${document.original_name}`} style={({ hovered }) => [styles.press, hovered && styles.hover]}><Card style={styles.card}><View style={styles.top}><View style={styles.fileIcon}><Text style={styles.fileText}>{document.document_type === 'boleto' ? 'R$' : 'PDF'}</Text></View><View style={styles.meta}><Text numberOfLines={1} style={styles.name}>{document.original_name}</Text><Text style={styles.info}>{typeLabel[document.document_type] || 'Documento'} · {date(document.created_at)}</Text></View><Badge status={document.status} /></View>{document.analysis_summary ? <Text numberOfLines={2} style={styles.summary}>{document.analysis_summary}</Text> : null}{onDelete ? <Pressable onPress={onDelete} accessibilityLabel={`Excluir ${document.original_name}`} hitSlop={10}><Text style={styles.delete}>Excluir</Text></Pressable> : null}</Card></Pressable>; }
const styles = StyleSheet.create({ press: { borderRadius: 22 }, hover: { transform: [{ translateY: -4 }] }, card: { gap: spacing.sm }, top: { flexDirection: 'row', alignItems: 'center', gap: spacing.sm }, fileIcon: { width: 40, height: 40, borderRadius: 12, backgroundColor: '#3B82F622', alignItems: 'center', justifyContent: 'center' }, fileText: { color: colors.primary, fontSize: 10, fontWeight: '800' }, meta: { flex: 1, minWidth: 0 }, name: { color: colors.text, fontWeight: '700', fontSize: 15 }, info: { color: colors.muted, fontSize: 12, marginTop: 3 }, summary: { color: colors.muted, ...typography.body }, delete: { color: colors.error, fontSize: 12, fontWeight: '700', alignSelf: 'flex-end' } });
