import React, { useState } from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { Button, Screen } from '../components/ui';
import { colors, common, spacing } from '../theme';
const slides = [{ icon: '▤', title: 'Documentos sem juridiquês.', text: 'Envie um arquivo e receba um resumo simples, com pontos importantes em destaque.' }, { icon: '◷', title: 'Prazos que não passam.', text: 'A IA encontra datas importantes para você agir no momento certo.' }, { icon: '✓', title: 'Decida com clareza.', text: 'Custos, riscos e próximas ações em uma experiência segura e privada.' }];
export default function Onboarding({ done }) { const [page, setPage] = useState(0); const slide = slides[page]; return <Screen style={styles.screen}><View style={styles.hero}><View style={styles.icon}><Text style={styles.iconText}>{slide.icon}</Text></View><Text style={common.title}>{slide.title}</Text><Text style={[common.subtitle, styles.text]}>{slide.text}</Text></View><View style={styles.bottom}><View style={styles.dots}>{slides.map((_, i) => <View key={i} style={[styles.dot, i === page && styles.active]} />)}</View><Button title={page === 2 ? 'Começar agora' : 'Continuar'} onPress={() => page === 2 ? done() : setPage(page + 1)} /><Button title="Pular" variant="ghost" onPress={done} /></View></Screen>; }
const styles = StyleSheet.create({ screen: { justifyContent: 'space-between', flexGrow: 1 }, hero: { flex: 1, justifyContent: 'center', gap: spacing.md }, icon: { width: 88, height: 88, borderRadius: 28, backgroundColor: '#3B82F622', justifyContent: 'center', alignItems: 'center' }, iconText: { color: colors.primary, fontSize: 42 }, text: { fontSize: 17 }, bottom: { gap: spacing.md }, dots: { flexDirection: 'row', gap: 8, justifyContent: 'center' }, dot: { width: 8, height: 8, borderRadius: 4, backgroundColor: colors.border }, active: { width: 25, backgroundColor: colors.primary } });
